# Base URL of the LetMessage API (no trailing slash).
# Local dev leaves this empty so Vite proxies /api and /socket.io to :5000.
VITE_API_URL=

# Production example:
# VITE_API_URL=https://letmessage-api.onrender.com
